"""Pydantic schemas — contract-first data models.

Defines all input/output formats shared between the API layer
and the core engine.  These schemas are the single source of
truth for the JSON contract with the Rust backend.
"""
