[package]
name = "gateway"
version = "0.1.0"
edition = "2021"

[dependencies]
serialport = "4.8.1"
chrono = { version = "0.4", features = ["clock"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
