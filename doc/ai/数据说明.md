# 数据说明

## 当前 baseline 的标签生成规则

当前第一版模型不是目标检测模型，而是图像分类 baseline。

为了快速完成第一阶段闭环，我们将带框标注（detection labels）转换成整图分类标签，规则如下：

1. 空标签文件：记为 `HealthyLeaf`
2. 单图中有一个或多个框，且类别相同：记为该类别
3. 单图中存在多个类别：取出现次数最多的类别作为整图标签

说明：
- 这是一条“先跑通系统”的工程路线，不代表后续最终任务形态。
- 如果后续升级为病斑定位、多目标检测或多标签识别，将另开模型路线，不直接复用当前分类输出。

