# ---------------------------------------------------------------
# Development dependencies — NOT included in production image
# ---------------------------------------------------------------
# Inherits all runtime deps:
-r requirements.txt

# PyTorch (local dev uses default index which may include CUDA)
torch
torchvision

# Training & evaluation
scikit-learn
tqdm
matplotlib

# Testing
pytest
httpx
